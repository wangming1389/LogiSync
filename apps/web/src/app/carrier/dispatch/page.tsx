import CarrierDispatchClient from './components/CarrierDispatchClient';

// Server Component shell. All interactive logic lives in
// `components/CarrierDispatchClient.tsx` (a Client Component).
export default function Page() {
	return <CarrierDispatchClient />;
}
