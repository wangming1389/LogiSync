import LoginClient from './components/LoginClient';

// Server Component shell. All interactive logic lives in
// `components/LoginClient.tsx` (a Client Component).
export default function Page() {
	return <LoginClient />;
}
