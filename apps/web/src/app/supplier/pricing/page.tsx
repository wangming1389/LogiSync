import SupplierPricingClient from './components/SupplierPricingClient';

// Server Component shell. All interactive logic lives in
// `components/SupplierPricingClient.tsx` (a Client Component).
export default function Page() {
	return <SupplierPricingClient />;
}
